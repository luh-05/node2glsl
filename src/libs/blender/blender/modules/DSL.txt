=== VERSION 2 ===

auto a = context->GetConstant<uint32_t>("a"); // a = 5

vec.AddLines({
  "Demo Module:",
  "beep boop... "
}, false);
vec.Add("Testing DSL\n");
vec.AddLine();

vec.AddLinesF({
  {"Constant 'a' is: {}", a},
  {"Constant 'a' == 4: {}, a == 4 ? "true" : "false"},
  {"Left port 'value0' resolves to: {}", this->GetLeftPort("value0")}
});

if (context->GetConstant<bool>("b")) {
  vec.AddLineF({"Constant 'b' is {}!", true});
}
